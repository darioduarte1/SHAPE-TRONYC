"""
Configuration for the profiles app.

This module defines the application configuration for the profiles app in Django.
"""
from django.apps import AppConfig

class ProfilesConfig(AppConfig):
    """
    Configuration class for the profiles app.

    This class sets the name of the application to 'profiles'.
    """
    name = 'backend.profiles'
